<resources>

    <style name="PaperText" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">@drawable/paper_text_color_selector</item>
        <item name="android:typeface">monospace</item>
        <item name="android:textSize">20sp</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:background">@drawable/paper_selector</item>
    </style>

    <style name="FormLabel" parent="@android:style/TextAppearance.Large">
        <item name="android:typeface">monospace</item>
        <item name="android:gravity">left</item>
        <item name="android:layout_margin">8dp</item>
    </style>

</resources>